/dist/
/appnative/rsrc_windows_*.syso

# Local build output. The published build lives in downloads/ as a zip.
/preview/
*.exe

# Model weights are never committed: they are fetched by the application at
# first use, into %APPDATA%\FlashFitAI\models.
*.gguf
